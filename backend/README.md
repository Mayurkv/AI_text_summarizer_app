# AI_text_summarizer_app
The user can provide a text and the AI can summarize the text.

# Steps to setup the application
Create a virtual environment and install the dependencies from the requirements.txt.
